onvif-zeep>=0.2.12

# kallon_watchdog.py (Phase 4):
requests>=2.31
smbus2>=0.4.3
# Jetson.GPIO is NVIDIA-provided and Linux-only; ignore on Windows dev workstations.
Jetson.GPIO>=2.1.6 ; sys_platform == "linux"
