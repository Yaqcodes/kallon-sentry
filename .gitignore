__pycache__/
*.py[cod]
*.pyo
.Python
.venv/
venv/
.env
.env.*
*.pcap
*.pcapng
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/
*.user
bin/
obj/
*.bundle

# Per-device secrets (live on Jetson under /etc/kallon or /etc/wireguard — never in git)
device.env
!device.env.example
alert.key
*.private
/etc/wireguard/

# Keys, tokens, VPN material
*.pem
*.key
*token*
*Token*
wg-keys*
.wg-keys*
*@*

# Editor / package artifacts
*.swp
*.assert

# Local systemd units copied from examples (paths differ per machine)
deploy/kallon-ptz-daemon.service
deploy/kallon-watchdog.service

# Factory output from kallon-fulfill-order (contains enrollment tokens)
fulfillment-output/
